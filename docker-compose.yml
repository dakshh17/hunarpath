
# ═══════════════════════════════════════════════════════════════════════════════
# HunarPath AI — Multi-Container Stack
#
#   db       →  PostgreSQL 16 + pgvector (port 5432)
#   backend  →  FastAPI AI engine         (port 8000)
#   web      →  Next.js buyer dashboard   (port 3000)
# ═══════════════════════════════════════════════════════════════════════════════

services:
  # ── PostgreSQL + pgvector ─────────────────────────────────────────────────
  db:
    image: pgvector/pgvector:pg16
    container_name: hunarpath-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: hunarpath
      POSTGRES_PASSWORD: hunarpath_secret
      POSTGRES_DB: hunarpath
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - hunarpath-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hunarpath -d hunarpath"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  # ── FastAPI Backend ───────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hunarpath-backend
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://hunarpath:hunarpath_secret@db:5432/hunarpath
      DATABASE_URL_SYNC: postgresql+psycopg2://hunarpath:hunarpath_secret@db:5432/hunarpath
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      SQL_ECHO: "0"
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - backend-uploads:/app/uploads
    networks:
      - hunarpath-net
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; r = httpx.get('http://localhost:8000/healthz'); assert r.status_code == 200"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ── Next.js Web Dashboard ─────────────────────────────────────────────────
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: hunarpath-web
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: http://backend:8000
      NODE_ENV: production
    ports:
      - "3000:3000"
    networks:
      - hunarpath-net
    depends_on:
      backend:
        condition: service_healthy

# ── Shared network ──────────────────────────────────────────────────────────
networks:
  hunarpath-net:
    driver: bridge

# ── Persistent volumes ──────────────────────────────────────────────────────
volumes:
  pgdata:
  backend-uploads:
